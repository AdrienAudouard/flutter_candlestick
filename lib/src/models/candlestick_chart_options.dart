class CandlestickChartOptions {
  /// If it's set to true the graph will be horizontally scrollable, if not all candlesticks
  /// will be displayed within the container
  final bool isScrollable;

  const CandlestickChartOptions({this.isScrollable = false});
}
